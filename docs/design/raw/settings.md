# Raw blab — settings

## 2026-09-06 (code)
Make the settings page. One thing I want in there is "add a league" with the name, start and end month, and the trophies that are part of that league. Everything else you can make. Also the settings page should be an overlay on the page you are on.

Make the settings page an overlay that goes over the top of the page, not along the side.

Make the start and end months a dropdown selection box.

Also make the icon on the browser tab the same as the logo.

Wire all the things that need wiring (reduce motion, export, import, delete all; keep state across refresh).

Make it so the user can choose a profile picture for their account.
